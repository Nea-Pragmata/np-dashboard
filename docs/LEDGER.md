# LEDGER — shared agent state (NP Dashboard build)

Append-only working log across build agents. Decisions of record go to Notion (additively); this file is temporary coordination state per CLAUDE.md.

## Status

- **Current milestone:** M13 NP Admin BUILT (all 7 pages + dark AdminShell, compiles 0/0). Security gate CLEAN (0 critical/high — scoped byråansatt cannot create businesses / edit agency_members / widen own scope / read out-of-scope; no superuser token in client; no config secrets exposed). Validation PASS except 1 gating defect (BusinessDrawer module editor crashed on open: `bind:checked={undefined}` — modules $state started {}). FIXED (init all MODULE_KEYS=false up front); re-validating. Then commit M13 → M14 mobil-finish (LAST). M0–M12 committed. Booking-time = UTC wall-clock.
- OPEN (needs superuser MCP — see MCP note below): packages/addon_services/agency_campaigns List+View rule is `published = true` (for the public website), which ALSO hides UNPUBLISHED rows from byråeier in NP Admin › Pakker & priser (can't see/re-publish the unpublished Tømrer-pakken). Fix: List+View → `published = true || <BYRÅMEDLEM :am expr>` (agency sees all, public sees published; C/U/D stays BE). Needs security re-review (read-widening for agency only — low risk).
- **MCP SUPERUSER SESSION HIJACKED:** a security subagent's `auth_with_password` (as a regular user) on the SHARED pocketbase MCP client overrode the superuser session, so collection-admin ops (get_collection/update_collection) now return "not allowed". Blocks the packages-rule fix AND the data-pollution cleanup until the MCP re-auths as superuser (restart the MCP server, or a superuser re-login). Reconfirms the M0 F3 rule: MCP writes must be isolated; agents doing auth_with_password must not share the admin client.
- **social_posts RULE CHANGE (M8, DEPLOYED + security-gate verified 0 findings — resolves the M0-flagged approval item):** updateRule is now `BY(business) OR (@request.auth.id != "" && business = @request.auth.business && status = "pending_approval" && @request.body.status = "approved" && @request.body.business:isset = false && @request.body.channel:isset = false && @request.body.content:isset = false && @request.body.thumbnail:isset = false && @request.body.scheduled_at:isset = false)`. Adversarial probes confirmed: customer can ONLY flip own pending_approval→approved (content change/published/cross-tenant all rejected 404). Update `docs/pb-schema.md` §3 social_posts + Notion §3 additively. pb-schema-export.json is stale by this one rule.
- OPEN (Notion additive): no ad-metrics data model → Annonser shows honest empty state instead of Figma's budget KPIs (needs an ads_metrics collection or read-from-integrations.config decision). Frisør modules.ads=false so Annonser only shows for Bil 1 (seed reality vs Figma drawing all 4 marketing tabs).
- Parallel-validation lesson: Chrome network-emulation (offline) is GLOBAL/shared across the instance — concurrent validators toggling it corrupt each other's error-state tests. For parallel batches, treat the offline error-state check as best-effort; the app + root +error.svelte boundaries now exist so leaf/layout errors render gracefully. Also: the dev-mode "cold offline navigation → bare 500" is a Vite chunk-fetch artifact, not a prod defect.
- DATA POLLUTION (needs user cleanup or explicit delete permission — auto-mode blocked agent delete of shared pre-existing records): Frisør has test-leftover records → customers `1i36th8m3agy4df` (Sofie Demo) + `8c610zned99wuqx` (Kari Kontakt) + inquiry `xj6mnr87k68itf1` (Kari Kontakt, FKs the customer) + a stray link_click (~17:39, makes clicks_30d read 21 not 20). Delete inquiry first, then both customers; drop the stray link_click to restore seed 10 customers / badge 3 / clicks 20.
- M14 POLISH BACKLOG (accumulated): (1) card radius is rounded-xl ~16px app-wide vs the «radius 12» design rule — normalize to radius-lg 12px. (2) AppShell sidebar is fixed 248px with no responsive collapse; «Skjul sidepanel» button has no onclick; mobile <768 needs bottom tabs + hamburger (Figma 283:5424/285:5493). (3) loading skeletons only fire on invalidation (SPA mounts after load) — fine, note it.
- OPEN (Notion additive): M11 needs a `businesses.domain`/public_url field to wire «Se nettsiden»/«Åpne profilen» (currently fall back to Google search); M11 forms card deferred to its own feature; ai_job_runs SEO line only visible to agency viewers (expand:job is agency-only) — customers still get seo_review_date. SMS-request template says «Frisør Oslo» vs business «Oslo Frisør» (seed template drift).
- PARALLEL BUILD PROTOCOL (batch milestones): each builder writes ONLY its own route dir + its own local components; MUST NOT edit shared files (StatusBadge.svelte, format.ts, modules.ts, layout.css, LEDGER.md) — put page-local format helpers in the route file, and return the LEDGER entry + any shared-change request as TEXT for the orchestrator to apply sequentially. Validators use Chrome offline-emulation for error states (NEVER stop the shared PocketBase), unique test-record names, and restore all data. StatusBadge is pre-extended with all collection statuses by the orchestrator.
- Known non-blocking issues (documented, not yet fixed): (a) hard page reload while PB is fully down logs the user out (root auth.init clears token on any authRefresh failure incl. network) instead of showing the error state — acceptable degradation; in-session client-nav error state works. (b) benign `net::ERR_ABORTED` on some mutations from realtime + invalidateAll double-refetch (SDK auto-cancel; cosmetic). (c) minor a11y: reply Textarea + a few inputs lack id/name (aria-label present). Revisit in M14 polish.
- M0 result: 30 base collections + 5 views + users, full rules matrix (155/155), 4 demo tenants seeded, probe artifacts cleaned (60 link_events). `pocketbase` SDK + `pocketbase-typegen` installed; `bun run check` green. Types in `src/lib/pocketbase-types.ts`.
- Plan: `~/.claude/plans/oppdraget-bygg-np-dashboard-reactive-gizmo.md`
- Schema fasit (working copy of Notion v2): `docs/pb-schema.md`
- PocketBase: `http://127.0.0.1:8090` (Docker). App and MCP use DIFFERENT hostnames by design (app → 127.0.0.1, MCP container → host.docker.internal). Do not "fix" either.

## Demo credentials (seed)

All seeded users share password `Demo1234!`.

| Login | Role |
|---|---|
| admin@npweb.no | Byråeier (agency owner, all businesses) |
| ansatt@npweb.no | Byråansatt (scoped: Frisør Oslo + Moen Bygg) |
| eier@frisor-oslo.no | Owner, Frisør Oslo (all modules) |
| ansatt@frisor-oslo.no | Staff, Frisør Oslo |
| eier@bil1.no | Owner, Bil 1 Trondheim |
| eier@moenbygg.no | Owner, Moen Bygg |
| eier@byggbo.no | Owner, Bygg & Bo AS (no modules → reduced menu) |

## Deferred server hooks (TODO — do not silently implement client-side workarounds beyond these)

1. subscription→modules sync — NP Admin UI writes both `subscriptions` and `businesses.modules` via PB batch API (milestone 13)
2. customer dedup + `customer` relation on public booking/inquiry create
3. deposit calculation + Vipps callback (deposit fields blocked in public create rule)
4. reviews sync from Google/Facebook (create locked; demo data seeded as superuser)
5. last-owner lockout guard on agency_members
6. review_request SMS scheduling

## Decisions / deviations log

- 2026-07-07: ai_job_runs customer read branch scopes on `show_status_to_client = true && business = @request.auth.business` (Notion literal said `status = "ok"` but field is named `result`) — to be reflected additively in Notion.
- 2026-07-07: social_posts Update stays BY-only in M0; customer "godkjenn" flow (pending_approval→approved) may need a narrow customer rule branch in milestone 8 — must pass security gate S1 re-review then.
- 2026-07-07: Server hooks deferred (list above) — approved direction in plan.
- 2026-07-07 (phase A schema DONE): users updated + all 30 base collections created via MCP import, all API rules null (superuser-only) incl. users' previous defaults — rules come in phase B. Collection ids are `pbc_np_*`. Deviations: `customers` UNIQUE(business, email) created as PARTIAL unique (`WHERE email != ''`) — fasit says plain unique, but email is optional and multiple customers without email in same business would collide (same pattern as PB's own auth email index); to be reflected additively in Notion. `bookings.payment_ref` partial unique created exactly as specified (SQLite supports it). Multi-relations maxSelect 999, multi-file fields maxSelect 99 (fasit had no limits). Views (fasit §4) intentionally not created in phase A.
- 2026-07-07 (phase B rules DONE): API rules set on all 31 collections per fasit §3 matrix, expanding EB/EE/BM/BY/BE verbatim from §2 (aliases `:am`/`:eier` kept, all `@request.body.*:isset` guards kept). No rule was rejected by PB validation; zero semantic changes needed. Locked (null) rules confirmed: link_events U/D, reviews C/D, site_status C/U, ai_job_runs C/U/D. Special cases applied: businesses uses `id` in BY(); users Create uses BY(@request.body.business); agency_tasks uses footnote-¹ combined expression on all five ops; ai_job_runs List/View = BY OR `(show_status_to_client = true && business = @request.auth.business)` (footnote ²). Verified by dumping all collections and diffing 155 rules (31×5) against the expected matrix — 155/155 exact match (script kept in session scratchpad). Views (§4) still pending — they carry their own List/View rules and are NOT covered by phase B.

- 2026-07-07 (phase C seed DONE): all demo data seeded as superuser via MCP batch API (batch enabled in PB settings, maxRequests 200). Counts: packages 4, addon_services 4, agency_campaigns 2, businesses 4, users 7, agency_members 2, subscriptions 4, categories 10, products 15 (6+6+3), attribute_schemas 11 (2+6+3), resources 2, booking_settings 1, customers 20 (10+6+4), bookings 15 (Frisør, week 2026-07-06…07-11, 4 today), inquiries 15 (5+4+6), link_pages 1, links 3, link_events 60, campaigns 7 (6+1), social_posts 3, content_templates 3, reviews 11 (6+3+2), pages 7 (3+2+2), forms 1, site_status 4, report_settings 2, integrations 5, agency_tasks 4, ai_jobs 5, ai_job_runs 12. waitlist_entries intentionally 0 (empty-state demo). **Figma-over-fasit deviations (per workflow instruction):** package names/prices from Figma packages frame (Kun nettside 990 / Frisør-pakken 2490 / Bilforhandler-pakken 3490 / Tømrer-pakken 2990, sistnevnte published=false) instead of Basis/Vekst/Komplett; 4 addons (SMS-pakke, Annonsedrift, AI-jobber [unpublished], Ekstra innholdsside); 2 agency_campaigns (Sommerkampanje −50 %, Gratis oppstart −4900 avsluttet april); module counts per Figma admin list (Frisør 8, Bil 6, Moen 5, Bygg & Bo 2 — fasit said Bygg & Bo 0); user names per Figma: eier@frisor-oslo.no = «Anne Berg» (ikke Frida Holm), ansatt@frisor-oslo.no = «Kari Hansen» (ikke Jonas Lie), admin@npweb.no = «Mats Aune» (ikke Nora Prytz; Figma-avatar «MA»); 5 ai_jobs per Figma (incl. content_suggestions + alt_text; seo kun Frisør); reviews-KPI-tall (126 anmeldelser, 24 bedrifter osv.) er design-fiksjon og kan ikke nås med seed-data. **File fields left EMPTY** (logo, product images, avatars, social thumbnails, inquiry attachments — incl. Kari Lunde «VEDLEGG (3)»): MCP create takes JSON only, no multipart upload — UI must render placeholders. Booking times seeded literally as UTC («09:00» → 09:00Z); if UI renders Europe/Oslo local time they display +2h — decide in milestone 5 whether to shift seed or render UTC.

- 2026-07-07 (phase D views DONE): all 5 view collections created per fasit §4 via MCP `import_collections` (MCP `create_collection` lacks type "view"/viewQuery; import path same as phase A, ids `pbc_np_*`). SQL taken verbatim from §4 — no dialect adjustments needed (json_extract, correlated json_each, `value = true` all accepted). Rules exactly per spec: public_business_profiles + public_booking_config List/View `""`; integration_status EB OR BY; stats_overview `id = @request.auth.business` OR BY(id); admin_business_overview BY(id) (never plain BM). Superuser row counts verified: public_business_profiles 4, integration_status 5, public_booking_config 1, stats_overview 4 (Frisør: visits_30d 40, bookings_30d 15, new_inquiries 3, clicks_30d 20), admin_business_overview 4 (module_count 8/6/5/2 matches Figma). `bun run typegen` re-run — view types now in `src/lib/pocketbase-types.ts`.

- 2026-07-07 (phase F rule fixes DONE — blocker + security finding fixed, 1 finding dismissed): builder agent applied two rule corrections via MCP `update_collection` (superuser), NO fields/indexes touched. **(F1, fixes phase E blocker):** `@collection.agency_members:am.allowed_businesses:length = 0` → `:length ?= 0` in ALL 102 occurrences across 28 collections (25 base incl. users + views integration_status/stats_overview/admin_business_overview; also the agency_tasks footnote-¹ expression). Plain `=` triggers PB multi-match ("ALL agency_members rows must have empty allowed_businesses"), so ansatt's scoped row broke empty=all for admin. This corrects broken logic — it does NOT widen access (probes below). Fasit §2 BYRÅ(business) + footnote ¹ corrected in `docs/pb-schema.md`; same additive correction needed in Notion §2. **(F2, fixes security finding HIGH "anonymous can read agency accounts"):** users List `business = @request.auth.business` → `(@request.auth.id != "" && business = @request.auth.business)`, users View same guard on the middle clause (`id = @request.auth.id` branch kept). Root cause: guests have `@request.auth.business = ""` which matched the two agency accounts (`business` empty) and exposed name/email/role of admin@npweb.no + ansatt@npweb.no. Pure tightening, matches the EB pattern used everywhere else. **Verification (REST curl with per-user tokens, NOT via MCP client; re-ran phase E checks 4+5):** admin@npweb.no → 4 businesses / 4 subscriptions / 4 admin_business_overview / 7 users. ansatt@npweb.no → exactly 2 (Frisør+Moen) on all three + 5 users (no Bil 1 — no escalation from `?=`). eier@frisor-oslo.no → 1 business, 2 users (own only), 0 integrations, 1 stats row; GET other-tenant user and agency user → 404. Guest → users list 0 rows, GET usradminnpweb01 404 (was 200 before fix); products/subscriptions public behavior unchanged (14 active / 0). Full rule diff: 102/102 patched fields exact, 0 residual `:length = 0`, 0 unintended rule changes (script in session scratchpad). `docs/pb-schema-export.json` re-dumped (41 collections). No typegen needed (rules only, no field changes). **(F3, DISMISSED as false positive for schema):** "intermittent cross-tenant write bypass under concurrent MCP batch" — deployed rules verified correct and identical to fasit; the two rogue writes were an auth-context race in the shared single-client MCP test harness, non-reproducible via isolated calls or the normal per-session REST API. No rule was changed (nothing to fix, and any change would have weakened or noised the matrix). Standing actions kept: security probes through MCP must be isolated sequential calls; milestone 2+ SvelteKit server MUST create a fresh PocketBase client per request (never a shared module-level authed client) — added to Open items.

- 2026-07-07 (phase E verification DONE — 1 BLOCKER found): empirical rule tests with seeded users (MCP auth_with_password) + guest curl. PASS: tenant isolation for eier@frisor-oslo.no (customers/bookings/inquiries own-only; cross-tenant filters → 0 rows), integrations hidden from customers while integration_status view works (no config exposed), stats_overview exactly 1 own row (visits 40, bookings 15+), owner business-update guards (name OK; modules/status/slug/type and other-tenant update denied), reviews create locked + reply_text-only update OK, ansatt@npweb.no scoping exact (2 businesses, no Bil 1 access), guest reads (products active-only, packages published-only, attribute_schemas public, content_templates/customers/bookings/inquiries 0 rows), public booking create (pending-only, deposit/confirmed rejected). **FAIL (blocker): admin@npweb.no (allowed_businesses = [] = "all") sees 0 businesses/subscriptions/admin_business_overview.** Root cause (probe-verified with superuser filter emulation): fasit §2 BYRÅ(business) branch `@collection.agency_members:am.allowed_businesses:length = 0` uses plain `=`, which PocketBase evaluates with multi-match ("ALL joined agency_members rows must satisfy") — any scoped member (ansatt has 2 allowed) breaks the empty=all branch for everyone. Validated fix: `:length = 0` → `:length ?= 0` (probes: admin → 4 businesses, ansatt → still exactly 2, no escalation). Affects 102 rule occurrences across 28 collections. Fix NOT applied (permission gate blocked schema writes for the verification agent, correctly per S1 gate) — must be applied by a builder agent + security re-review, then re-run phase E checks 4+5. Fasit §2 + Notion need additive correction. Test artifacts cleaned: guest test booking deleted, review reply_text reverted. Schema dump: `docs/pb-schema-export.json` (41 collections, superuser list-collections dump, parity spot-verified via MCP get_collection).

## Tooling — PocketBase types

- PB collection types are GENERATED, not hand-written. Run `bun run typegen` (wrapper `scripts/typegen.mjs` → `pocketbase-typegen`) to (re)write `src/lib/pocketbase-types.ts`. Reads `POCKETBASE_URL`/`POCKETBASE_ADMIN_EMAIL`/`POCKETBASE_ADMIN_PASSWORD` from `.env` (gitignored). `.env.example` documents the vars.
- **Rule for all build agents:** after ANY schema change (fields/collections), re-run `bun run typegen` and import types from `$lib/pocketbase-types` — never hand-author collection record types. App reads `PUBLIC_PB_URL` (add to `.env`, milestone 2).

- 2026-07-07 (M2 auth + app shell BUILT — awaits independent Chrome validation): SPA mode (`src/routes/+layout.ts` `export const ssr = false`) + root load `await auth.init()`. Root `+layout.svelte` mounts `<Toaster>` (svelte-sonner) once. Guards use `await parent()` then module-singleton `auth.*`: `(auth)/+layout.ts` → authed redirects to /oversikt; `(app)/+layout.ts` → unauthed → /logg-inn, resolves `{ business, businesses, modules }`, `depends('app:tenant')`; `(admin)/+layout.ts` → non-agency → /oversikt. Root `/` `+page.ts` bounces to /oversikt or /logg-inn. New files: routes `(auth)/logg-inn/+page.svelte` (login, Field + Input + one black `Button size=lg`, `auth.login`→`goto('/oversikt')`, error→`toast.error(pbError)`; matches Figma 281:5370), `(app)/+layout.svelte` (AppShell + grouped DRIFT/VEKST/NETTSTED menu from `filterNav(modules)`, Henvendelser badge via `$effect` re-counting `inquiries status=new` on business change, `depends`-driven), `(app)/oversikt/+page.svelte` (placeholder), `(admin)/np-admin/+page.svelte` (stub). Components in `src/lib/components/layout/`: AppShell (custom 248px sidebar + 56px topbar + scroll content, NOT shadcn sidebar block), NavItem (`page` from `$app/state`, active=white surface+1px border per Figma 134:58), TenantSwitcher (agency-only dropdown → `tenant.set` + `invalidate('app:tenant')`), Topbar (visual ⌘K search + bell + user-menu dropdown logout). NP Admin nav entry gated on `auth.isAgency`. `format.ts` gained `initials`/`businessTypeLabel`/`roleLabel`. `bun run check` 0/0, `bun run build` OK (adapter-auto "no production env" warning expected). Client-only pb usage everywhere (ssr=false) → no shared-server-client auth-bleed risk (F3). **Figma/instruction deviations flagged to validation agent:** (1) task text said nav active = "blue accent" but Figma 134:58 + CLAUDE.md ("blå aksent kun på mikropunkter") → active = white surface + 1px `--border-default` + medium text; blue kept to focus-ring(2px) + badge; (2) reduced-module tenant (Bygg & Bo) renders always-on Oversikt+Statistikk+Nettsted&SEO+Innstillinger, but Figma 136:474 shows only Oversikt+Nettsted&SEO+Innstillinger (no Statistikk) — followed `utils/modules.ts` (Statistikk keyed `always`) per task's "menu from modules.ts" instruction; reconcile whether Statistikk should be module-gated; (3) Innstillinger declared group NETTSTED in modules.ts but Figma pins it to the sidebar footer next to NP Admin — rendered in footer without mutating modules.ts (consider a `footer` flag); (4) shadcn `Input` primitive left at radius `rounded-lg`(12px)/`border-input`(#D4D4D8) vs Figma field radius-sm(6px)/#E4E4E7, height nudged to h-9(36px) — primitive-level, defer to design gate; (5) ⌘K search + notifications bell are visual-only affordances this milestone.

- 2026-07-07 (M2 RETTING — console/network fixes from Chrome validation, check #9): fixed the two reproducible normal-flow issues; no security change, no schema/typegen. **(R1, dev fetch warning on tenant switch):** `src/routes/(app)/+layout.ts` `getFullList` ran on the module-global `pb` (SDK default `window.fetch`), so a client-side `invalidate('app:tenant')` tripped SvelteKit's "using window.fetch" warning. Fix: destructured `fetch` from `load` and passed it through as `getFullList({ sort: 'name', fetch })` — PB `SendOptions.fetch` is honoured by the SDK (`(options.fetch||fetch)`, v0.27.0). Uses SvelteKit's load-`fetch` instead of the shared global client (aligns with M2 hard-requirement note). SPA/`ssr=false` so no auth-bleed either way. **(R2, `net::ERR_ABORTED` on duplicate `agency_members` GET every login/auth-init):** root cause was a double `#sync` in `src/lib/stores/auth.svelte.ts` — `authWithPassword`/`authRefresh` save the token which fires `authStore.onChange` → `void #sync()` while `login()`/`init()` also `await #sync()` for the same auth change; two identical `#loadAgencyMember` `getFirstListItem` reads race and the SDK auto-cancels the loser (retry returns 200, benign but a failed request). Fix: coalesced `#sync()` behind a shared `#syncInFlight` promise (`??=` + `.finally` clear), inner work moved to `#syncOnce()`; concurrent callers now share one in-flight sync ⇒ exactly one `agency_members` request per auth change, no abort. `bun run check` 0/0, `bun run build` OK. **Figma deviations from the same validation run: DISMISSED, not silently coded** (per CLAUDE.md "uklarheter avklares ved å oppdatere Notion additivt — aldri stille antakelser i koden"): (a) no-module tenant (Bygg & Bo) shows Statistikk — `modules.ts` keys it `always` by documented intent; whether it should be module-gated is the open reconciliation already logged as M2 deviation #2 (needs Notion decision + there is no `statistics` module key to gate on) — a design change touching every tenant, out of scope for a console/network RETTING. (b) no-module tenant shows Henvendelser — CORRECT behaviour: seed gives Bygg & Bo `inquiries: true` (phase C: "Bygg & Bo 2 modules per Figma admin list"); the menu faithfully renders modules, so any mismatch with Figma 136:474 is a seed/Figma internal inconsistency to fix in seed/Notion, not code. (c) business-type label "Bedrift" vs Figma "Byggevarer" — seed `type = 'annet'` → `businessTypeLabel` fallback; `byggevarer` is not a `businesses.type` enum value (only frisor/bilforhandler/tomrer/annet), so this is a seed/schema+Notion item, not a client bug. (d) login node 281:5380 — validator itself noted this is not a deviation (281:5380 is the e-post sub-component of the matched 281:5370 frame). The one 400 on the wrong-password test is the expected error-toast path, not a defect.

- 2026-07-07 (M2 follow-up — Statistikk gating + Bygg & Bo re-seed, user-approved decision; resolves M2 deviation #2 and the RETTING (a) reconciliation): **Decision (approved by user):** «Statistikk» is now MODULE-GATED, not always-on — it shows only when the tenant has at least one operational module enabled (any `businesses.modules` key true among booking/catalog/inquiries/customers/campaigns/links/social/ads/reviews/waitlist). «Oversikt», «Nettsted & SEO» and «Innstillinger» stay always-on. **Code (`src/lib/utils/modules.ts`):** introduced `MODULE_KEYS` (the 10 operational keys) as the single source of truth and derived `ModuleKey` from it; added `hasAnyModule(modules)` helper; added a `'any-module'` gate marker (new `NavGate = ModuleKey | 'always' | 'any-module'`); changed the Statistikk nav item's key from `'always'` → `'any-module'`; `filterNav` now returns `'always'` items unconditionally, `'any-module'` items when `hasAnyModule` is true, and specific-key items when that module is on. No shadcn primitive touched, runes syntax kept, UI text unchanged (Norwegian bokmål). `(app)/+layout.svelte` unchanged — its empty-group filter already drops the now-empty VEKST header for pure-website tenants. **Data (PocketBase, value change only — NOT schema, no typegen):** re-seeded Bygg & Bo AS (`businesses` id `bizbyggbo000001`, slug `bygg-og-bo`) `modules` from `{inquiries:true, links:true, rest false}` to ALL 10 keys false, making it a true pure-website tenant. Write confirmed via MCP `update_record` (response shows all 10 false). This supersedes the phase-C seed note "Bygg & Bo 2 modules per Figma admin list" — reverts to fasit's original "Bygg & Bo 0 modules" intent. **Verification:** `bun run check` 0 errors / 0 warnings. Chrome smoke-check against running dev server (`http://localhost:5173`): eier@byggbo.no → sidebar EXACTLY «Oversikt», «Nettsted & SEO», «Innstillinger» (NETTSTED group header only; no Statistikk, no operational items, no DRIFT/VEKST headers) — matches Figma frame 136:474. eier@frisor-oslo.no (all modules) → «Statistikk» present under VEKST with the full operational menu (Katalog, Booking, Henvendelser, Kunder, Markedsføring, Anmeldelser, Statistikk). **Notion — needs additive correction:** the datamodell line «Statistikk always-on» conflicts with the reduced-menu frame 136:474; resolved as **Statistikk gated behind any operational module** — record additively in «PocketBase – Databaseoppsett» (also note the Bygg & Bo modules reversion to 0).

- 2026-07-07 (M3 Oversikt BUILT — awaits independent Chrome validation): built the customer dashboard home INSIDE the existing (app) shell. New shared components in `src/lib/components/shared/`: **KpiCard** (label 11/16 caps, value 30/36 semibold tabular-nums via formatKr/formatNumber, optional trend up/down/neutral + optional sparkline Snippet — no shadow, radius-lg), **StatusBadge** (central `{collection,status}→{bokmål label, tone}` map covering bookings/inquiries/reviews + derived `site_status`; renders shadcn Badge as a rounded-full pill w/ dot, tone→NP tokens success/warning/error/info/neutral via twMerge override of the default variant — verified computed bg/text resolve to #ECFDF5/#047857 etc.), **EmptyState** (wraps shadcn Empty; icon+title+description+optional action snippet, bokmål). NO chart added — `stats_overview` has only current counts (no time series), so SparkLine skipped this milestone and KPI trend deltas omitted (Figma's +12 % etc. are design fiction per phase-C note). New helpers in `format.ts`: `formatNumber` (space-grouped int) and `formatTime` (HH:mm rendered in **UTC** to match the UTC-literal seed times → «Dagens avtaler» shows 09:00/11:30/14:00 as in Figma; the definitive booking-tz decision stays deferred to M5). Route files: `(app)/oversikt/+page.ts` (client load, ssr=false; parallel reads w/ `requestKey:null` + load-`fetch`: stats_overview by `business=id`, today's bookings [UTC day window, expand product,customer] only if booking module, reviews status=new count only if reviews module, site_status; `firstOrNull` swallows only 404, network → `error(503,'Får ikke kontakt med serveren')`), `+page.svelte` (greeting «God morgen, {firstName}» + prose subtitle w/ long-weekday date; module-gated KPI grid 4→2→1; «Krever handling» section w/ per-row links [nye henvendelser→/henvendelser, bookinger venter→/booking, anmeldelser→/anmeldelser] or EmptyState; Snarveier quick-actions [module-gated, «Rediger åpningstider» always]; Drift/site-status pills; «Dagens avtaler» list only if booking module, excludes done+cancelled; `navigating`-gated loading skeleton), `+error.svelte` (NP error card + «Prøv igjen»→invalidateAll). At most ONE black primary button rule honoured: page uses ZERO (all buttons outline/ghost, matching Figma 139:277). `bun run check` 0/0, `bun run build` OK (adapter-auto warning expected). **Chrome-verified against running app (both tenants):** Frisør Oslo (eier@frisor-oslo.no) → KPIs 40/15/3/20, follow-ups «3 nye henvendelser»/«1 booking venter»/«2 nye anmeldelser», Snarveier ×4, Drift ×3 «I orden», Dagens avtaler 3 rows (09:00 Silje/Bekreftet, 11:30 Jonas/Bekreftet, 14:00 Mia/Venter — 08:00 done row excluded); Bygg & Bo (eier@byggbo.no, 0 modules) → 2 KPIs (Besøk 0, Klikk 0), «Krever handling» empty-state «Alt er à jour», Snarveier only «Rediger åpningstider», Drift ×3, NO «Dagens avtaler», NO «Se full statistikk». Zero console errors/warnings, no failed PB calls. **Deviations flagged (NOT silently coded around — for validation/design gate + Notion):** (1) section title «Krever handling» kept verbatim from Figma 140:413 rather than the task's prose name «Til oppfølging i dag» (design binding); (2) KPI trend deltas omitted — no previous-period data in `stats_overview`; (3) greeting subtitle uses long weekday «tirsdag 7. juli» (Figma prose) vs CLAUDE.md's short «man. 6. juli» convention for DATA/column dates — prose exception; (4) `formatTime` renders UTC wall-clock (see M5 tz decision); (5) duplicate `<h1>`: M2 `Topbar.svelte` renders the route title as `<h1>` and this page's greeting is also `<h1>` (Figma «Tittel/H1») → two h1s; recommend M2 topbar demote its title (out of M3 scope, not edited); (6) at 390 px the M2 shell does not yet collapse the 248 px sidebar (mobile shell = M14) so content is cramped — the page content itself is responsive (grid stacks 4→2→1). Added `.claude/launch.json` (bun run dev, port 5173) for the preview harness.

- 2026-07-07 (M4 Katalog BUILT — awaits independent Chrome validation): built the catalog (products/tjenester + kategorier) with the DYNAMIC-FIELDS core feature, inside the (app) shell. New route files under `src/routes/(app)/katalog/`: **`+page.ts`** (client load, ssr=false; parallel `getFullList` with `requestKey:null`+load-`fetch` for products [expand category, sort sort_order,name — owner sees all statuses incl. hidden/sold], categories [sort_order,name], attribute_schemas [sort_order]; derives `catalogSchemas` = schemas whose `show_in` includes 'catalog'; network → `error(503)`), **`+page.svelte`** (custom underline tab bar «Produkter & tjenester» | «Kategorier»; produkter = `<DataTable>` cols BILDE-placeholder/Navn/Kategori/Pris(right, tabular-nums)/Status/⋯, client-side søk+kategori+status filtre; kategorier = card rows w/ grip handle + «{n} produkt(er)» + ⋯; ConfirmDialog delete for both; category-delete guarded when it still has products), **`ProductDrawer.svelte`** (create+edit; standard fields Navn/Kategori/Beskrivelse/Pris-type/Pris/Prisenhet + visibility/bookable/featured switches PLUS `<DynamicForm>` on the business's catalog `attribute_schemas` bound to a working copy of `products.attributes`; section header «Bransjefelter · {industryLabel}»; create sends `business`+derived `slug`, update omits `business` per customer rule), **`CategoryDrawer.svelte`** (create+edit; Navn, auto-slug), **`+error.svelte`**. Reused infra DataTable/Drawer/ConfirmDialog/DynamicForm/DynamicField/StatusBadge/EmptyState unchanged. Shared additions: `format.ts` gained `formatCatalogPrice` (fixed→«1 250 kr», from→«Fra 1 250 kr», on_request→«På forespørsel», +«/time»·«/m²» unit suffix; stk→none), `slugify` (æøå transliteration), `industryLabel` (frisor→«Frisør» etc, shorter than businessTypeLabel); **StatusBadge STATUS_MAP gained `products`** (active→«Aktiv»/success, hidden→«Skjult»/neutral, sold→«Solgt»/info). `bun run check` 0/0, `bun run build` OK (adapter-auto warning expected). **Chrome-verified against running app (both tenants):** Frisør Oslo (eier@frisor-oslo.no) list matches Figma 147:463 (6 rader, «Fra 1 200 kr»/«Fra 1 800 kr», «Skjeggtrim/Skjult»); edit-drawer renders «BRANSJEFELTER · FRISØR» → Varighet (minutter)=60 + Passer for=Dame (Figma 150:973). Bil 1 Trondheim (eier@bil1.no) — the SAME drawer renders «BRANSJEFELTER · BILFORHANDLER» → Årsmodell/Km-stand/Drivstoff/Girkasse/Farge/Reg.nr, sorted by sort_order (requirement #5 met, ZERO industry branching). Required-field validation fires on empty submit incl. dynamic fields («Årsmodell/Km-stand/Drivstoff må fylles ut.», optional Girkasse/Farge/Reg.nr silent); Kategorier tab shows «SUV 3 produkter / Stasjonsvogn 2 produkter / Sedan 1 produkt» (Figma 150:603). Write-contract verified via REST with the bil owner token: CREATE w/ business → 200, UPDATE w/o business → OK, **UPDATE *with* business → 404 (customer update rule rejects it — confirms why the drawer omits `business` on update)**, DELETE → 204 (test row cleaned up, seed intact). Zero console errors; only pre-existing M2 `auth-refresh` window.fetch dev-warnings from root `auth.init` (not katalog). **Figma deviations flagged (NOT silently coded — for validation/design gate + Notion):** (1) **Table columns:** Figma 147:463 shows a «VARIGHET» column (the frisør `varighet_min` attribute) and no image column; a per-industry attribute column can't generalize without industry branching (violates requirement #5 — the whole thesis of this milestone), so followed the task's explicit column spec (thumb-placeholder/Navn/Kategori/Pris/Status/⋯). The thumb is a muted placeholder since seed `images` are empty. Reconcile in Notion whether the list should carry a dynamic attribute column. (2) **Drawer extra fields:** Beskrivelse, Prisenhet, «Kan bookes», «Fremhev på nettsiden» are task-required (`products` has these fields) but absent from Figma 150:973/152:1207 which show only Navn/Kategori/bransjefelter/Pris-type/Pris/«Synlig i katalogen». Kept the Figma «Synlig i katalogen» switch (maps status active↔hidden). (3) **«Solgt» not settable via the visibility switch** (Figma has no control for it) — a pre-existing `sold` product is preserved unless toggled visible; no seed product is `sold` so no data loss. StatusBadge still renders «Solgt» if present. (4) **Bransjefelt order:** rendered by `sort_order` per task instruction (frisør→Varighet then Passer for; bil→Årsmodell,Km-stand,Drivstoff,Girkasse,Farge,Reg.nr); Figma bil frame 152:1207 happens to show Km-stand before Årsmodell — followed sort_order (authoritative per task). (5) **Empty-state CTA** uses Figma «Nytt produkt» (title «Ingen produkter ennå», desc «Legg inn det første produktet, så vises det på nettsiden din med en gang.») rather than the task's suggested «Legg til ditt første produkt» — matched Figma 152:1574; the page's top-right «Nytt produkt» goes outline when the list is empty so the centered black CTA is the single primary (honours «maks én svart primærknapp»). (6) **Description shows raw HTML** — seed `description` is `<p>…</p>` HTMLString; the plain `<Textarea>` shows the tags. Acceptable for this milestone (user-typed descriptions are plain text); a rich-text editor is out of scope — flag for the nettside-contract/design gate. (7) **Category reorder:** grip handle rendered as a visual affordance but drag-to-reorder is not wired (sort_order editing deferred) — flag. (8) **Loading skeleton** wired via `navigating.to.pathname==='/katalog'` → DataTable `loading` branch (same pattern as M3 Oversikt); on SPA entry the page mounts after load so it mainly covers invalidations. Mutations use `invalidateAll()` after toast (sonner).

- 2026-07-07 (M4 Katalog RETTING — Chrome-validation fixes; no schema/typegen, dynamic-fields core untouched): fixed the 3 confirmed defects, dismissed 6 items as documented/out-of-scope. **FIXED (3):** **(1) ConfirmDialog stayed open after confirm → double-delete/404** (`src/lib/components/shared/ConfirmDialog.svelte`): root cause — in bits-ui 2.18.1 `AlertDialog.Action` (via `DialogActionState`) carries NO close-onclick (only `Cancel`/`DialogCloseState` does), so `handleConfirm` fired `onconfirm` but never closed; after a successful delete (204) the modal lingered and a second «Slett» re-deleted the removed id → `DELETE …/products/records/{id}` 404. Fix: `handleConfirm` now sets `open = false` (the `$bindable` prop) before calling `onconfirm`, so the dialog closes exactly like the Cancel path. This is a shared component — verified only the two katalog delete-confirm usages consume it. Transitively kills the reported network-404 (no lingering confirm button to re-click). **(2) search Input had no id/name** (`(app)/katalog/+page.svelte` «Søk i katalogen …»): added `name="katalog-sok"` + `type="search"` + `aria-label="Søk i katalogen"` (Input forwards restProps). Clears the DevTools a11y/autofill advisory and gives the field a real accessible name (placeholder ≠ label). **(3) empty Kategorier rendered TWO black primary buttons** (violates «maks én svart primærknapp per skjerm»): the Kategorier header «Ny kategori» button was always `variant=default` while the centered empty-CTA is also default → two primaries when `categories.length === 0`. Fix: header now `variant={categories.length > 0 ? 'default' : 'outline'}`, mirroring the already-correct Produkter header — empty ⇒ header outline + one primary CTA; non-empty ⇒ header is the single primary (no empty CTA shown). `bun run check` 0/0, `bun run build` OK (adapter-auto warning expected). **Chrome-verified against running app (eier@bil1.no / Bil 1 Trondheim):** created two throwaway products via REST → deleted each through the ⋯ → Slett → ConfirmDialog flow: confirm now deletes (server GET → 404) AND closes (dialog `data-state` open→closed→unmounted, `body pointer-events` restores to auto after the 0.1s exit anim), page stays interactive, no second-delete possible; seed intact (6 Bil products, no ZZZ leftovers). Cancel-path A/B confirmed the close mechanics are identical to bits-ui's own (so fix #1 introduces no new behavior). Search Input verified live (`name`/`type`/`aria-label` present). Kategorier header (3 seed categories) verified the single black primary (`rgb(24,24,27)`); empty-case is symmetric with the verified Produkter header. **DISMISSED (6, with reason — reconcile in Notion additively, not silently in code):** (a) **`auth-refresh` window.fetch dev-warning** (fires once per `invalidateAll`): pre-existing M2 root `auth.init()` using the module-global `pb` client; dev-only (absent in production build); katalog's own `+page.ts` already uses load-`fetch` correctly. Fixing it is an M2 root-layout change (auth.init must accept a load-`fetch`) — out of an M4 katalog RETTING's scope; logged as an M2 follow-up. (b) **VARIGHET vs BILDE column** (Figma 147:463): a per-industry attribute column can't generalize across tenants without industry branching — the entire thesis of M4 (requirement #5, zero branching); kept the generic thumb/Navn/Kategori/Pris/Status/⋯ spec. Reconcile in Notion whether the list should carry a configurable dynamic-attribute column. (c) **Product drawer extra fields** (Beskrivelse/Prisenhet/«Kan bookes»/«Fremhev»): task-required and backed by real `products` fields; Figma 150:973/152:1207 show a subset. (d) **Bil drawer field order** (Årsmodell before Km-stand): follows `attribute_schemas.sort_order` (authoritative per task); Figma 152:1207 shows the reverse. (e) **Beskrivelse shows raw HTML** in the Textarea: seed `description` is an HTMLString; a rich-text editor is out of scope — flagged for the nettside-contract/design gate. (f) **Category drag-to-reorder** not wired (grip is a visual affordance; `sort_order` editing deferred) — pre-existing M4 flag, not a regression.

- 2026-07-07 (M5 Booking BUILT — awaits independent Chrome validation): built the module-gated Booking page (Kalender + Avtaler) inside the (app) shell for tenants with `modules.booking` (Frisør Oslo). New route files under `src/routes/(app)/booking/`: **`week.ts`** (pure UTC date helpers — `weekStart`/`addDays`/`isoDate`/`parseIsoDate`/`minutesOfDay`/`hhmmToMinutes`/`isoWeekNumber` + `DAY_KEYS`/`OpeningHours`; ALL in UTC to honour the LEDGER booking-tz convention so day-column/hour bucketing stays aligned with `formatTime`), **`+page.ts`** (client load; module gate `if (!modules.booking) redirect(302,'/oversikt')`; week from `?uke=YYYY-MM-DD` normalised to its Monday, default = week containing `new Date()`; `depends('app:bookings')`; parallel `getFullList` w/ `requestKey:null`+load-`fetch`: bookings for `[monday,nextMonday)` half-open on `start` w/ `expand:product,staff,customer,resource` sort start, plus resources[active], bookable products, business users [staff], customers; network → `error(503)`), **`WeekCalendar.svelte`** (Mon–Sun grid, 52px time gutter + 7 day columns, hour rows across the union of opening-hours & booking span, 56px/hour; bookings absolutely-positioned by UTC start/end; **per-day overlap packing** into side-by-side lanes via first-fit per overlap-cluster [verified live: Wed 08-07 two 10:00 bookings render side-by-side]; each block = white card + 1px border + left **status stripe** coloured by StatusBadge tone [pending=warning amber / confirmed=success green / done=neutral grey / cancelled=error muted+opacity+line-through]; block shows `HH:mm` + customer_name + product; today column header blue + column bg `accent-blue-bg`), **`BookingDrawer.svelte`** (shared Drawer; CREATE = one black primary «Opprett avtale»: product Select [bookable] + date + Fra/Til time + customer_name/phone/email + optional staff/resource/notes → `pb.create` w/ `status='pending'`, denormalised customer_* text, business/product/start/end, NO customer relation / NO deposit — auto-fills end from product `varighet_min`; VIEW/EDIT = read-only summary [tjeneste, tid «tir. 7. juli, 09:00–10:00», telefon/e-post, **read-only Depositum** if `deposit_status` set — verified Silje shows «Betalt · 150 kr»] + status actions [pending→Bekreft, confirmed→Fullført, always Avlys] + editable Ansatt/Ressurs/«Koble til kunde»[customer relation]/Notat via `pb.update` sending ONLY staff/resource/customer/notes|status — NEVER business/deposit/payment), **`+error.svelte`** (503 card + «Prøv igjen»). **`+page.svelte`** orchestrates: «Kalender»|«Avtaler» underline tabs, shared toolbar [«I dag» + ◀▶ + week label «Uke 28 · 6. juli – 12. juli» + «Alle ansatte» staff filter], week nav via `goto('/booking?uke=…')`, KALENDER=WeekCalendar (+ centered empty overlay «Ingen avtaler denne uken» / «Del bookinglenken din …» / black «Ny avtale» when `bookings.length===0`, header CTA demotes to outline then per «maks én svart primærknapp»), AVTALER=`<DataTable>` (Tid «man. 6. juli 09:00» via formatDate+formatTime / Kunde / Tjeneste / Ansatt / Status badge / ⋯ [Åpne, Bekreft/Fullført, Avlys]) + client søk-på-kunde + status filter, sorted by start. **REALTIME:** a `$effect` subscribes `pb.collection('bookings').subscribe('*', …, {filter: business="…"})` → `invalidate('app:bookings')`; cleanup unsubscribes in the effect return; re-subscribes on business-id change; ignores subscribe/abort failures; one subscription per business (no duplicates). Reused DataTable/Drawer/StatusBadge/EmptyState/Skeleton unchanged; imported `statusMeta`/`StatusTone` from StatusBadge for the stripe tones. No new shared helpers needed (formatTime/formatDate/formatKr reused; NO change to `src/lib/utils/format.ts`). `bun run check` 0/0. **WRITE CONTRACT proven live via REST (eier@frisor-oslo.no token) BEFORE building:** create pending+denormalised+staff+resource+notes → 200; update staff/resource/notes → 200; update status+customer relation → 200; update w/ business → 404; update w/ deposit_status → 404; create w/ customer relation → 400. **Bookings Delete rule = BY (agency-only): a tenant owner CANNOT delete** — confirms the UI design «Avlys = status→cancelled, never delete» (there is no delete affordance). Test rows cleaned via admin@npweb.no (BY); seed re-verified 15. **Chrome-verified (eier@frisor-oslo.no, preview :5173):** calendar renders 15 bookings w/ correct stripes + overlap; today=TIR 7. blue; detail drawer opens w/ correct data + deposit; Avtaler table correct cols/format/sort; **empty week (Uke 27) shows the centered empty-calendar overlay + outline header CTA (matches Figma 163:1898)**; module gate verified — eier@byggbo.no (no booking module) hitting `/booking` → redirected to `/oversikt`. Zero console errors, zero failed PB calls. **Figma deviations flagged (NOT silently coded — for validation/design gate + Notion):** (1) **third «Innstillinger» tab** in Figma 158:1249/163:1382 NOT built — task said build only Kalender|Avtaler + «Do NOT build Innstillinger yet»; rendered two tabs. (2) **Stripe colours are SEMANTIC (StatusBadge tones) per explicit task instruction**, whereas Figma's literal stripes read dark/near-black for confirmed and amber for done/farging — followed the task's `pending=warning/confirmed=success/cancelled=error muted/done=neutral` mapping (clearer + reuses the tone tokens); reconcile the literal palette in Notion if the dark-confirmed look is intended. (3) **Tid column** uses «man. 6. juli 09:00» (task convention) instead of Figma's relative «I dag/I morgen 09:00» labels. (4) **«Visning: Uke» dropdown** (day/week view switch) omitted — only week view is in scope; flagged rather than shipping a dead control. (5) **«Neste 7 dager» range selector** on the Avtaler tab replaced by the shared week navigator (data is week-scoped: both tabs read the same loaded week), and the week navigator + «Alle ansatte» staff filter are shown on BOTH tabs (Figma shows the navigator only on Kalender). (6) **Staff filter «Alle ansatte»** implemented as a functional shared filter (applies to calendar + list) though not required by the task — matches Figma affordance. (7) **Grid hour range is data-driven** (union of opening_hours + bookings → 08–19 for the seed week incl. the 08:00 Tue booking & Thu 19:00 close), so it's taller than Figma's fixed 09–16; empty weeks fall back to opening-hours (09–19). (8) **Edit drawer does not reschedule time/product** (task scoped edit to status/staff/resource/notes/customer) — product/tid are read-only in edit; create covers new times. (9) **Cancel («Avlys») has no confirm dialog** — direct status→cancelled + toast (matches Figma, which shows no confirm); reconsider a ConfirmDialog if desired. (10) **Mobile 390 px:** the calendar's own `overflow-x-auto`(min-width 864px) scrolls and the header/toolbar stack, but the 248px sidebar still does not collapse — the SAME pre-existing M2 shell limitation logged for M3/M4 (mobile shell = M14), not a booking defect. **Booking-tz convention UNCHANGED & now load-bearing in M5:** times are stored/rendered as literal UTC wall-clock; create writes `\`${date}T${time}:00.000Z\`` so typed 09:00 round-trips through `formatTime` (UTC) as 09:00, and grid bucketing uses `getUTC*` — do NOT introduce Europe/Oslo conversion.

- 2026-07-07 (M5 Booking · Innstillinger BUILT — awaits independent Chrome validation): built the booking settings view as a SUB-ROUTE `src/routes/(app)/booking/innstillinger/` (task allowed sub-route OR 3rd tab; sub-route keeps the calendar page's client-state Kalender/Avtaler toggle intact and doesn't bloat its week-load). New files: **`+page.ts`** (client load; module gate `if (!modules.booking) redirect(302,'/oversikt')`; `depends('app:booking-settings')`; parallel `requestKey:null`+load-`fetch`: business re-fetched FRESH via `getOne` for `opening_hours` (the ONLY source, v2.2 — not cached layer copy), `booking_settings` via `getFullList` filter business `[0] ?? null` (avoids 404 noise; agency-only Create means a customer tenant may legitimately have no row), resources `getFullList` expand staff sort name, business users [staff]; network → `error(503)`; exported typed JSON shapes `Reminders`/`Deposit`/`WaitlistConfig`/`SettingsRow`/`ResourceRow`), **`+page.svelte`** (header «Booking» + subtitle mirrors calendar page; own 3-tab bar where Kalender=`<a href="/booking">` / Avtaler=`<a href="/booking?fane=avtaler">` / Innstillinger=active `<span>`; 2-col grid — LEFT: Åpningstider [7 day rows Mandag–Søndag, per-day open/close `type=time` Inputs + a Switch that flips `closed`; closed day renders «Stengt» text, seeded from `businesses.opening_hours`], Påminnelser [Switch SMS + «timer før» Select + Switch e-post + Switch oppfølging → `booking_settings.reminders`]; RIGHT: Betaling og depositum [Switch enable + percent number(%) + min_amount number(kr) + read-only Vipps method + Figma helper texts → `booking_settings.deposit` w/ `method:'vipps'` always], Venteliste og avbestilling [Switch enable + «Gratis avbestilling inntil» Select → `booking_settings.waitlist`], Ressurser [list Stol 1/Stol 2 «— {ansatt}» w/ ⋯ Rediger/Slett + «Legg til ressurs» outline btn]; a whole-form re-seed `$effect` reads `data` and writes form `$state` (never reads it → no loop; after save+invalidate the fresh DB values re-seed); ONE black primary «Lagre endringer» at bottom-right), **`ResourceDrawer.svelte`** (shared Drawer; create/edit name + optional staff Select + Aktiv Switch → `pb.create` w/ business (EB rule) OR `pb.update` OMITTING business (customer rule needs `business:isset=false`)), **`+error.svelte`** (503 card). Reused shared Drawer/EmptyState/ConfirmDialog/DropdownMenu/Field/Select/Switch/Input unchanged; NO change to `format.ts` or any shared component. **Also edited `booking/+page.svelte`** (minimal, additive — not a rebuild): added a 3rd tab `<a href="/booking/innstillinger">Innstillinger`, and init `activeTab` from `?fane=avtaler` so the settings page can link back to the Avtaler view (later Kalender/Avtaler toggles stay pure client state, no reload); swapped the tablist `role`/`aria-selected` for `aria-current="page"` since the bar now mixes tab-buttons + a route link. **SETTINGS WRITE CONTRACT proven live via REST (eier@frisor-oslo.no owner token) BEFORE building:** businesses PATCH `{opening_hours}` → 200, PATCH `{opening_hours,status}` → 404 (owner rule rejects status/slug/modules/type); booking_settings PATCH omit-business → 200, PATCH +business → 404; resources CREATE +business → 200, PATCH omit-business → 200, PATCH +business → 404, DELETE → 204. **booking_settings Create rule = BY(business) (agency-only)** — a customer tenant with NO row cannot self-create; the UI therefore disables the reminder/deposit/waitlist controls + shows «Kan ikke endres» when `settings` is null (Lagre still saves opening_hours). Seeded Frisør row exists so this branch is defensive only. **Chrome-verified (eier@frisor-oslo.no, preview :5173):** all 5 cards render matching Figma 163:1646 (Åpningstider 7 rows incl. Søndag «Stengt» toggle-off; deposit 20 %/min 100 kr/Vipps; Påminnelser SMS+e-post on, oppfølging off, «24 timer før»; Venteliste on «24 timer før»; Ressurser Stol 1 — Anne Berg / Stol 2 — Kari Hansen). **End-to-end save round-trip proven:** toggled «Oppfølging etter besøket» ON in the UI → «Lagre endringer» → DB `booking_settings.reminders.follow_up` flipped false→true, deposit/waitlist untouched, ZERO failed requests (both PATCHes 200 — no forbidden-field 400s); reverted follow_up→false to keep the seed intact. Tab wiring verified both directions. Zero console errors on the page (only the pre-existing root `auth-refresh` window.fetch warning, amplified by repeated manual re-auth during testing — NOT from this route's load, which threads load-`fetch`). **Seed hygiene:** removed a stray `ZZ Test stol` resource left by a prior session; resources now exactly Stol 1 + Stol 2. `bun run check` 0/0, `bun run build` OK (adapter-auto warning expected). **Figma deviations flagged (NOT silently coded — for validation/design gate + Notion):** (1) **«timer før» / «Gratis avbestilling inntil» rendered as Selects (2/4/12/24/48 timer)** matching Figma's dropdowns, though the task worded `hours_before` as a «number» — both persist a number; chose the drawn dropdown (better for the non-technical audience). (2) **Minstebeløp (`min_amount`) + read-only «Betalingsmetode: Vipps»** are in the task's data spec but NOT drawn in Figma 163:1646 (which shows only the percent + integration helper) — added minimally to satisfy the data model; reconcile whether they should be visible. (3) **Resources save immediately** via their own drawer/⋯ (create/edit/delete), separate from «Lagre endringer» (which saves opening_hours + reminders/deposit/waitlist) — matches Figma's per-row ⋯ + «Legg til ressurs» affordances. (4) **`method` fixed to `'vipps'`** on every deposit save (only option per fasit + Figma «Betales med Vipps eller kort» / integrations note). (5) **«Lagre endringer» is a plain right-aligned bar** (not sticky) — matches Figma's bottom-right placement. (6) **Mobile:** cards stack 2→1 (`lg:grid-cols-2`); the 248px sidebar still doesn't collapse — same pre-existing M2 shell limitation (mobile shell = M14), not a settings defect.

- 2026-07-07 (M5 Booking RETTING — Chrome-validation blocker fixed; no schema/typegen, no shared-component change): fixed the 1 confirmed CRITICAL blocker, dismissed the 5 Figma-deviation flags (all already logged as intentional M5 deviations). **FIXED (1) — create + edit-save were impossible from the UI** (`src/routes/(app)/booking/BookingDrawer.svelte`): the two `{#snippet footer()}` blocks were nested INSIDE the `{#if editing && booking}` / `{:else}` branches of `<Drawer>`. In Svelte 5 only snippets that are **direct children** of a component are hoisted into its props, so nesting them left the `footer` prop `undefined` → the shared Drawer's `{#if footer}` was false → neither the create «Opprett avtale»/«Avbryt» nor the edit «Lagre endringer»/«Lukk» buttons ever rendered (validator confirmed SheetContent had only 3 children, no submit button in the DOM). Fix: collapsed the two nested snippets into ONE `{#snippet footer()}` declared as a direct child of `<Drawer>` (after the body `{/if}`), which branches on `editing && booking` internally to render the edit pair vs the create pair — mirrors the proven-working `booking/innstillinger/ResourceDrawer.svelte` (footer as direct child → renders). Pure structural move: button labels, handlers (`createBooking`/`saveDetails`), `disabled={saving}`, and the create/update API payloads are byte-for-byte unchanged, so the already-live-proven write contracts (create → status=pending + denormalised customer_* + no deposit/customer; update → status/staff/resource/customer/notes only, never business/deposit/payment) are untouched. `bun run check` 0/0, `bun run build` OK (adapter-auto warning expected). Correctness established structurally + by direct analogy to ResourceDrawer (which the validator saw render its footer); independent Chrome re-validation of the now-present submit buttons is left to the validation agent per the milestone loop (retting agent does not validate own work). **DISMISSED (5 Figma deviations — NOT defects; each already recorded as an intentional deviation in the M5 Booking BUILT entry above, reconcile in Notion additively not in code):** (a) **«Visning: Uke» day/week view-switch dropdown omitted** — only week view is in scope; shipping a dead day-view control was rejected (M5 BUILT deviation #4). (b) **Calendar status stripes use semantic StatusBadge tones** (confirmed=success green / pending=warning amber / done=neutral / cancelled=error) rather than Figma's literal dark/near-black-for-confirmed stripes — per the explicit task instruction to use semantic tones (M5 BUILT deviation #2). (c) **«Tid» column + calendar labels use absolute «man. 6. juli 09:00»** instead of Figma's relative «I dag/I morgen 09:00» — task convention (M5 BUILT deviation #3). (d) **Innstillinger adds «Minstebeløp» + read-only «Betalingsmetode: Vipps»** not drawn in Figma 163:1646 — required by the task's data spec, added minimally (M5 · Innstillinger BUILT deviation #2). (e) **Avtaler tab shows the shared week navigator + «Alle ansatte» filter** (Figma draws the navigator only on Kalender) and replaces Figma's «Neste 7 dager» range selector — both tabs read the same week-scoped load, so a single shared navigator is coherent (M5 BUILT deviation #5). None of these block the milestone; they are open Notion-reconciliation items, not code changes.

- 2026-07-07 (M7 Kunder BUILT — awaits independent Chrome validation): built the module-gated customer register + loyalty inside the (app) shell for tenants with `modules.customers` (Frisør, Bil 1, **and Moen Bygg** — seed has `customers:true` for Moen, contradicting the task text's "Moen does NOT"; the page reads live `modules.customers` so the gate self-corrects; Bygg & Bo = false → redirected/​hidden). New route files under `src/routes/(app)/kunder/`: **`+page.ts`** (list load; module gate `if (!modules.customers) redirect(302,'/oversikt')`; `getFullList` filter business, sort `-last_visit,name`, `requestKey:null`+load-`fetch`; network→error(503); exports typed `PunchCard`/`Consents`/`CustomerRow` = `CustomersResponse<Consents,PunchCard>`), **`+page.svelte`** (Figma 182:2280: `<DataTable>` cols Kunde[avatar+name, stretched `<a href="/kunder/{id}">` as the row-open target]/Telefon[phone→email fallback]/Siste besøk[`formatDate`]/Besøk[right, tabular]/Klippekort[«{count} av {goal}» or green «Fullt!» chip when count≥goal, «—» when no card]/Samtykke[Mail icon when `consents.email===true`, Smartphone when `consents.sms===true`, «—» when neither]/⋯[Åpne kundekort·Rediger·Slett]; client søk[navn/telefon/e-post] + Samtykke filter[Alle/E-post/SMS/Ingen] + Sortering[Sist besøk/Navn]; ONE black «Ny kunde» primary [outline when list empty so the empty-CTA is the single primary]; empty+loading[skeleton via navigating]+error via DataTable states), **`+error.svelte`** (503 card + «Prøv igjen»), **`CustomerDrawer.svelte`** (create+edit form: Navn[req]/Telefon/E-post/Klippekort-mål[goal, default 10]/Belønning; create sends `business`+`consents:{email:false,sms:false}`+`punch_card`; update OMITS business [customer rule needs `business:isset=false`], preserves existing punch count, leaves consents untouched — consents are edited ONLY on the detail page), **`[id]/+page.ts`** (detail load; getOne + defensive tenant guard→404; parallel bookings[expand product, gated on modules.booking] + inquiries[gated on modules.inquiries] filtered `customer="{id}"`; 404/503 mapped), **`[id]/+page.svelte`** (Figma 186:2526: back-link + «{name}» H1 + «Kunde siden {månedÅr} · {n} besøk»; Rediger + [booking-gated] «Ny avtale» — both outline, ZERO black primary per «maks én»; LEFT col: Kontakt[phone/email/hairline/SAMTYKKER two Switches reflecting `consents.email/sms` EXACTLY + «Samtykke registrert {formatDateFull}»] + Klippekort[«Klipp {count} av {goal}» + «{remaining} igjen»(info)/«Fullt!»(success) chip + `goal` circles [first `count` filled dark, rest outline] + reward_text + «Registrer klipp»[increment, capped at goal, toast reward when full] + «Nullstill kort»[reset→0, shown when full]; no-card state → «Legg til klippekort»]; RIGHT col: Kommende avtale[soonest future pending/confirmed booking, booking-module-gated] + Historikk[merged bookings+inquiries table Dato/Type[Booking neutral·Henvendelse warning chip]/Detaljer/Beløp[=expanded product price for bookings, «–» for inquiries], empty state]), **`[id]/+error.svelte`** (404 «Fant ikke kunden»→«Til kundelisten» vs 503→«Prøv igjen»). **CONSENTS ARE GDPR-HONEST:** every read is `=== true`; a missing/false flag never renders an icon or an ON toggle; toggling writes the full consents JSON and stamps `consents.registered = new Date().toISOString()` the first time anything is granted (local-mirror `$state` re-seeded from server via `$effect`, optimistic with revert-on-error). **Shared additions:** `format.ts` gained `formatMonthYear` («mars 2024») + `formatDateFull` («12. mars 2024»); NO shared component changed. **M6 deep-link resolved (minimal edit):** `henvendelser/+page.svelte` inquiry-detail «Kunde» link `href="/kunder"` → `href="/kunder/{selected.expand.customer.id}"` — nothing else in M6 touched. `bun run check` 0/0, `bun run build` OK (adapter-auto warning expected). **Chrome-verified against a fresh dev server (own port 5199 — the :5173/:5174/:5190 servers belong to other chats/worktrees and lacked these routes; eier@frisor-oslo.no):** list renders 12+ rows matching Figma (Sara Moe «Fullt!» green chip; Espen/Emma «—» samtykke; Ola smartphone-only; Jonas/Nora mail-only; «man. 6. juli»/«søn. 28. juni» dates); Silje detail = both consents ON + «Samtykke registrert 12. mars 2024» + 7/10 circles; Espen detail = both consents OFF + «Ingen samtykker registrert ennå.» + 6/10 + Historikk table (Skjeggtrim 350 kr / Herreklipp 650 kr, Beløp from product price); **«Registrer klipp» round-trip 6→7 av 10** (write+invalidate, toast); **consent toggle round-trip** e-post OFF→ON stamped «Samtykke registrert 7. juli 2026» while SMS stayed OFF (never falsely granted). Zero console errors/warnings on all views. Espen's seed restored (count 6, consents both false) after the mutation tests. **Figma deviations flagged (NOT silently coded — for validation/design gate + Notion):** (1) **«Siste besøk» uses `formatDate` «man. 6. juli»** (task instruction + CLAUDE.md date convention) instead of Figma's weekday-less «28. juni» — reconcile which date format the customer register should use. (2) **Detail = a full sub-route `/kunder/[id]`** (task allowed drawer OR page; Figma 186:2526 is a full two-column page that a 440px drawer can't hold) — row-open + the M6 deep-link both navigate to `/kunder/{id}`. (3) **Historikk «Beløp» is derived from the expanded product's list price** (bookings store no charged amount) — honest approximation; Figma also mixes a «Kampanje»(link_events) row type which is OUT of scope (would need a link_events join) — Historikk shows bookings + inquiries only. (4) **Consents live ONLY on the detail page** (not in the create/edit drawer) — the drawer creates with both OFF per task; granting is the explicit detail-page action. (5) **Samtykke list column shows an icon only for a GRANTED channel** (matches Figma: absent when false, «—» when none) rather than the task's literal «filled vs muted» wording — chose the safer GDPR reading (a muted icon risks reading as granted). (6) **Delete lives on the list ⋯ menu only** (owner-only EE rule; Figma detail header has no delete) — staff get a friendly bokmål toast on 403/404. (7) **Stray test customers** «Kari Kontakt»/«Sofie Demo» (0 besøk, «—») seen in the shared PB are from another chat's session, not created here; they render correctly (null punch_card/last_visit handled). Booking-tz convention unchanged (upcoming-appointment time via UTC `formatTime`). Not committed (task: do not git commit).

- 2026-07-08 (M14 Mobil-finish BUILT — the final cross-cutting milestone; awaits independent Chrome validation): made the app work on mobile (<768px = Tailwind `md`) and applied the accumulated polish. **RESPONSIVE APP SHELL (backlog #2):** reworked `src/lib/components/layout/AppShell.svelte` from a fixed 248px sidebar into a responsive frame — desktop (≥`md`) UNCHANGED (248px `aside` + 56px `topbar` snippets + scroll `main`); mobile hides the `aside`, swaps the desktop topbar for a `mobileHeader` snippet, and pins a `tabbar` snippet in-flow at the foot of the column (in-flow ⇒ never overlaps content, so no bottom padding hack needed) + `main` gets `overflow-x-hidden`. New props: `mobileHeader`, `tabbar` (both `md:hidden`). **New components in `src/lib/components/layout/`:** `MobileTabBar.svelte` (bottom tab bar, Figma 285:5493 — Oversikt first, ≤2 module-gated primary tabs, «Meny» button last; active = dark strong text/icon + medium weight [Figma 285:5522], inactive = muted; Henvendelser badge stays blue accent; safe-area `padding-bottom: max(0.75rem, env(safe-area-inset-bottom))`); `MobileMenu.svelte` (off-canvas left `Sheet` at `w-[88vw] max-w-[360px]` — business header + the SAME `filterNav` groups as the desktop sidebar [so module-aware for free] as 48px rows w/ chevron-right + badges, divider before Innstillinger, NP Admin when `auth.isAgency`, user card + logout; `afterNavigate`→close); `Fab.svelte` (fixed round black «+» bottom-right at `bottom: calc(5.25rem + env(safe-area-inset-bottom))`, `md:hidden`, no box-shadow per design rule — the black surface is the mobile primary). **MODULE-GATING (tab bar):** added `mobilePrimaryNav(modules)` to `src/lib/utils/modules.ts` — priority order booking→inquiries→catalog→customers→campaigns→reviews, first 2 enabled; falls back to «Nettsted & SEO» when a tenant has zero operational modules. Verified live: Frisør (all modules) → Oversikt/Booking/Henvendelser(badge 9)/Meny; Bygg & Bo (0 modules) → Oversikt/Nettsted & SEO/Meny (exactly the spec example). **`(app)/+layout.svelte`:** wired `menuOpen` state, a `mobileHeader` snippet (logo N + business name/type + bell + hamburger «Åpne meny»), a `tabbar` snippet (`<MobileTabBar>`), and `<MobileMenu bind:open={menuOpen}>`; both the hamburger and the «Meny» tab set `menuOpen=true`. **BOOKING MOBILE DAY-VIEW:** new `booking/BookingDayView.svelte` (single-day agenda — day navigator ◀▶ + «I dag» + `first-letter:uppercase` day label; vertical time list w/ status stripe + time range + customer + product + StatusBadge; empty state, no black CTA so the FAB stays the sole primary). `booking/+page.svelte` now renders `<BookingDayView class="md:hidden">` (day-filtered, staff-filtered) + `<WeekCalendar class="hidden md:block">` (desktop week grid UNTOUCHED); the shared week-nav toolbar cluster is `hidden md:flex` on the Kalender tab (the day nav replaces it on mobile); FAB wired to «Ny avtale»; header primary is `hidden md:inline-flex`. `booking/+page.ts` gained a `?dag=YYYY-MM-DD` param → `selectedDayIso` (dag-in-week else today-in-week else Monday). Day nav is client-side within the loaded week (`dayOverride` $state) and `goto(?uke&dag)` when crossing the week boundary; `dayOverride` is cleared only when the loaded WEEK changes, so a same-week realtime `invalidate('app:bookings')` never snaps the agenda off the viewed day. `newBookingDate` now = `selectedDayIso` (mobile new-booking defaults to the viewed day; desktop identical). **FAB on create pages (mobile only):** Booking→«Ny avtale», Katalog→«Nytt produkt»/«Ny kategori» (follows active tab), Kunder→«Ny kunde»; on each the header primary is `hidden md:inline-flex` so the FAB is the single black mobile primary («maks én svart primærknapp»). Henvendelser has no create → no FAB (correct). **CARD RADIUS POLISH (backlog #1):** normalized card/panel/surface CONTAINERS from `rounded-xl` (~16px) to `rounded-lg` (=12px per NP tokens) app-wide — bulk `perl` replace of the exact substring `rounded-xl border border-border bg-card` across 25 files (route cards, cardClass strings, DataTable, +error cards, admin cards, loading-skeleton cards) + manual `empty.svelte` (EmptyState wrapper) + the `nettsted` accent-blue info banner. **Left `rounded-xl`** intentionally: `ui/dialog`, `ui/alert-dialog`, `ui/command` (centered modals/overlays, same family as the protected Sheet/Drawer), `ui/card/card.svelte` (unused primitive — only a local TS type named `Card` exists), and the 72px logo THUMBNAIL in BedriftsprofilTab (not a card container). No buttons/inputs/badges/avatars touched. **DoD sweep:** empty/loading/error states + bokmål intact after the shell rework (spot-checked Bygg & Bo «Alt er à jour» empty state on mobile; booking agenda empty state; Katalog/Kunder tables no horizontal overflow at 390px). `bun run check` 0/0, `bun run build` OK (adapter-auto warning expected). **Chrome-verified (preview :5173, own dev server; eier@frisor-oslo.no + eier@byggbo.no):** DESKTOP 1280px UNREGRESSED — `aside` display flex width 248px, mobile tab bar hidden, booking WEEK grid + week navigator + header «Ny avtale» black primary render, FAB hidden. MOBILE 390px — mobile topbar (logo/business/bell/hamburger), stacked KPI cards, bottom tab bar (Oversikt active dark, Henvendelser blue «9», Meny), hamburger opens the full-nav sheet matching Figma 285:5493 (DRIFT/VEKST/NETTSTED groups, chevrons, divider, user card; NP Admin correctly ABSENT for a customer owner), booking day agenda (Onsdag 8. juli + I dag + 3 bookings w/ green/amber stripes + status badges), FAB opens «Ny avtale» drawer («Opprett avtale» present), NO horizontal overflow at 390px on Oversikt/Booking/Katalog/Kunder (`main.scrollWidth == clientWidth == 390`), zero console errors. **Deviations flagged (NOT silently coded — reconcile in Notion additively):** (1) **Active tab uses Figma's dark strong text/icon (285:5522), NOT a full «blue accent»** despite the task wording — Figma draws the active tab dark and CLAUDE.md pins «blå aksent kun på mikropunkter»; the blue is kept for the Henvendelser badge (a micro-point). Consistent with the M2 nav-active decision (white surface + medium text, not blue). (2) **Mobile menu is an ~88vw off-canvas left drawer with a scrim sliver, not a literal fullscreen overlay** — the task explicitly allows «Sheet/drawer»; a scrimmed drawer is the standard mobile pattern and keeps a dismiss affordance; Figma 285:5493 draws it full-bleed. (3) **Desktop sidebar «Skjul sidepanel» (PanelLeft) button left non-functional** — the requirement pins desktop as UNCHANGED (no validated collapse behavior), so the wired hamburger is the MOBILE one (`Åpne meny`); a desktop collapse toggle would add state + regression risk out of scope. (4) **Mobile topbar shows a hamburger + bell** (Figma 285:5494 draws logo/business/bell/avatar with no hamburger — the menu opens via the «Meny» tab there); added the hamburger per the task and dropped the decorative avatar (its logout lives in the menu sheet's user card). (5) **`ui/empty` + card-container radius edits touch generated primitives** — one-token `rounded-xl`→`rounded-lg` normalization to satisfy the radius-12 design rule (same class of customization as the M2 Input radius note), not a hand-authored rewrite. Not committed (task: do not git commit).

## Open items

- 2026-07-07 (M4 follow-up DONE — resolves dismissed item (a) from M4 RETTING): threaded SvelteKit load `fetch` through `auth.init(fetch)` → `authRefresh`/`getOne`/`getFirstListItem` via a `#loadFetch` field (set only during init) in `auth.svelte.ts`; root `+layout.ts` passes `fetch`. Kills the recurring dev-only "using window.fetch" console warning (M2/M3/M4 validations). Re-confirmed in Chrome: warning gone, login OK for customer + agency. check + build green. (Note: `#loadFetch` is cleared after init, so `authStore.onChange`-triggered syncs outside a load correctly use global fetch.)
- Reflect the M2-follow-up decision additively in Notion «PocketBase – Databaseoppsett»: «Statistikk» is module-gated (shown only when ≥1 operational module is on), NOT always-on as the datamodell currently states; and Bygg & Bo AS is a 0-module pure-website tenant (supersedes the phase-C "2 modules per Figma admin list" seed note). Resolves M2 deviation #2.
- Regenerate `pocketbase-types.ts` once milestone-0 workflow finishes creating all collections (initial run captured only system collections).
- Reflect phase F rule corrections additively in Notion «PocketBase – Databaseoppsett» (§2 BYRÅ(business) `:length ?= 0`, footnote ¹, users List/View auth guard) — see phase F ledger entry.
- Milestone 2+ HARD REQUIREMENT (from dismissed finding F3): SvelteKit server code must instantiate a fresh PocketBase client per request with per-request auth. Never a shared module-level authenticated `pb` instance — concurrent multi-tenant requests would bleed auth context server-side.
