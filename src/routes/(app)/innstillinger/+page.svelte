<!--
	Placeholder for `/innstillinger`. The load in +page.ts always redirects to the
	default tab, so this component never actually renders — it only exists so
	SvelteKit treats `/innstillinger` as a page route.
-->
